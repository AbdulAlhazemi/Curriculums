🧪 **5. تدريب: أضف دالة (Method)**

بالنظر إلى هذه الدالة البانية:

```javascript
function Book(title, author) {
  this.title = title;
  this.author = author;
}
```
أضف دالة `read()` إلى `Book.prototype` تقوم بطباعة “Reading [title]”.

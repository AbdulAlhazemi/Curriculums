🧪 **5. تدريب: أنشئ صنفًا فرعيًا**

أنشئ صنف `SportsCar` يرث من `Car`، ويضيف خاصية `turbo`، ويتجاوز دالة `drive()` لتضمين معلومات `turbo`.

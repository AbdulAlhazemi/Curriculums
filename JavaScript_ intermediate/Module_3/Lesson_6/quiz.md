✅ **اختبار قصير للتحقق**
1.	أي دالة تقوم بتصفية المصفوفات؟
    أ) `.map()`
    ب) `.filter()` ✅
    ج) `.reduce()`
    د) `.sort()`
2.	أي دالة تقوم بتحويل كل عنصر؟
    أ) `.filter()`
    ب) `.map()` ✅
    ج) `.some()`
    د) `.reverse()`
3.	كيف تحصل على قيمة واحدة من مصفوفة؟
    أ) `.reduce()` ✅
    ب) `.find()`
    ج) `.every()`
    د) `.forEach()`
